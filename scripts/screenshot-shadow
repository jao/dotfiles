# screenshot-shadow on|off

$killall = 1
if [ "$1" == "on" ]; then
  defaults write com.apple.screencapture disable-shadow -bool true
elif [ "$1" == "off" ]; then
  defaults write com.apple.screencapture disable-shadow -bool false
else
  $killall = 0
fi
[ $killall == 1 ] && killall SystemUIServer
